# requirements for Hugging Face Spaces / local web demo
# 部署到 HF Space 时把这份文件改名为 requirements.txt 上传（或在 Space 里手工同步内容）
# 本地跑：pip install -r requirements-app.txt && python app.py

# 核心 skill
jieba>=0.42

# Web UI
gradio>=4.36,<5.0

# LLM 客户端（DeepSeek 用 OpenAI 兼容 API）
openai>=1.30

# HF Space 通常自带 fastapi/uvicorn，本地需要时取消注释：
# fastapi
# uvicorn
